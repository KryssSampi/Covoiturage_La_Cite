<?xml version="1.0" encoding="utf-8" ?>
<!--
  Features/statistiques/Views/Components/BadgeGridView.xaml
  BindingContext : BadgeGridDisplayModel
-->
<ContentView
    x:Class="Covoiturage_la_cite__App_Mobile_.Features.statistiques.Views.Components.BadgeGridView"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:dm="clr-namespace:Covoiturage_la_cite__App_Mobile_.Features.statistiques.DisplayModels;assembly=Covoiturage_la_cite_(App_Mobile)"
    x:DataType="dm:BadgeGridDisplayModel">

    <VerticalStackLayout Spacing="0">
        <Grid Padding="16,18,16,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <HorizontalStackLayout Grid.Column="0" Spacing="8">
                <Label Text="🏅" FontSize="14" VerticalOptions="Center"/>
                <Label Text="Mes badges" FontFamily="OpenSansSemibold" FontSize="14"
                       FontAttributes="Bold" TextColor="#0D1624" VerticalOptions="Center"/>
            </HorizontalStackLayout>
            <Label Grid.Column="1"
                   Text="{Binding ObtainedCount, StringFormat='{0} obtenus'}"
                   FontSize="12" TextColor="#7A879A"/>
        </Grid>

        <CollectionView ItemsSource="{Binding Badges}" SelectionMode="None" Margin="16,0">
            <CollectionView.ItemsLayout>
                <GridItemsLayout Orientation="Vertical" Span="3"
                                 HorizontalItemSpacing="10" VerticalItemSpacing="10"/>
            </CollectionView.ItemsLayout>
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="dm:BadgeDisplayModel">
                    <Frame BackgroundColor="White" CornerRadius="14"
                           BorderColor="#12000000" HasShadow="False" Padding="8,12">
                        <Frame.Triggers>
                            <DataTrigger TargetType="Frame" Binding="{Binding IsLocked}" Value="True">
                                <Setter Property="Opacity" Value="0.45"/>
                            </DataTrigger>
                        </Frame.Triggers>
                        <VerticalStackLayout Spacing="6" HorizontalOptions="Center">
                            <Label Text="{Binding Emoji}" FontSize="26"
                                   HorizontalTextAlignment="Center"/>
                            <Label Text="{Binding Name}"
                                   FontFamily="OpenSansSemibold" FontSize="11"
                                   FontAttributes="Bold" TextColor="#0D1624"
                                   HorizontalTextAlignment="Center" LineBreakMode="WordWrap"/>
                            <Label Text="{Binding DateLabel}"
                                   FontSize="10" TextColor="#7A879A"
                                   HorizontalTextAlignment="Center"
                                   IsVisible="{Binding DateLabel, Converter={StaticResource StringNotEmptyToBool}}"/>
                            <Label Text="{Binding ProgressLabel}"
                                   FontSize="10" TextColor="#1A56CC" FontAttributes="Bold"
                                   HorizontalTextAlignment="Center" LineBreakMode="WordWrap"
                                   IsVisible="{Binding ProgressLabel, Converter={StaticResource StringNotEmptyToBool}}"/>
                        </VerticalStackLayout>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <Frame BackgroundColor="#E8F0FE" CornerRadius="16" BorderColor="Transparent"
               Padding="12,10" Margin="16,12,16,0" HasShadow="False">
            <HorizontalStackLayout Spacing="10">
                <Label Text="💡" FontSize="14" VerticalOptions="Start" Margin="0,1,0,0"/>
                <Label Text="{Binding InsightText}"
                       FontFamily="OpenSansRegular" FontSize="12"
                       TextColor="#1A56CC" LineBreakMode="WordWrap" HorizontalOptions="Fill"/>
            </HorizontalStackLayout>
        </Frame>
    </VerticalStackLayout>
</ContentView>
